# walker — app launcher config
# CSS theme is written to ~/.local/share/walker/themes/current.css by theme-set
theme = "current"

force_keyboard_focus = true
selection_wrap = true
hide_action_hints = true

[placeholders]
"default" = { input = " Search...", list = "No Results" }

[keybinds]
close = ["Escape"]
