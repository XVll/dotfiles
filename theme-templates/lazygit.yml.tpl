gui:
  theme:
    activeBorderColor:
      - "{{ accent }}"
      - bold
    inactiveBorderColor:
      - "{{ comment }}"
    selectedLineBgColor:
      - "{{ bg_alt }}"
    cherryPickedCommitBgColor:
      - "{{ color6 }}"
    cherryPickedCommitFgColor:
      - "{{ accent }}"
    unstagedChangesColor:
      - "{{ color1 }}"
    defaultFgColor:
      - "{{ foreground }}"
