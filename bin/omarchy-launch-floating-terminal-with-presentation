#!/bin/bash

# Launch a floating terminal, execute the command passed in, and end with the omarchy-show-done presentation.

cmd="$*"
exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.omarchy.terminal --title=Terminal -e bash -c "$cmd; if (( \$? != 130 )); then omarchy-show-done; fi"
